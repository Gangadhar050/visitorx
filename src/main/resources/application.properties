spring.datasource.url=jdbc:mysql://localhost:3306/visitorx
spring.datasource.username=root
spring.datasource.password=test


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# File Upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT
jwt.secret=visitor-x-super-secret-key-change-in-prod-32chars
jwt.expiration=86400000

#This is for development, change to your frontend URL in production
app.visitor.form-url=http://localhost:3000/visitor-register

# In production, you would set this to your actual frontend domain, for example:
#app.visitor.form-url=https://your-frontend-domain.com/visitor-register
